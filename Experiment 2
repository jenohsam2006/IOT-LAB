void setup()
{
  pinMode (7, OUTPUT);
  pinMode (2, INPUT); 
}
void loop()
{
  int bstate = digitalRead(2);
  digitalWrite(7, LOW); 
  if (bstate == HIGH) 
  {
    digitalWrite(7, HIGH); 
  }
  else
  {
    digitalWrite(7, LOW); 
  }
}
